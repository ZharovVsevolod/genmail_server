[project]
name = "genmail-server-services"
version = "0.1.0"
description = "United services connection part of genmail on server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "omegaconf>=2.3.0",
    "pydantic>=2.11.9",
    "python-dotenv>=1.1.1",
]
